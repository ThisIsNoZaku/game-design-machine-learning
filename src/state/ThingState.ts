/**
 * Runtime state of anything that exists within the game: players, entities, locations, etc.
 */
// TODO: Turn this into a dynamic type, where different things can have different state shapes, and the shape is defined by the game definition.
export interface ThingState {

}