{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/boardml/core/phases.schema.json",
  "title": "Phases",
  "type": "object",
  "required": ["initial", "nodes"],
  "properties": {
    "initial": {"type": "string"},
    "nodes": {"type": "object", "items": {"$ref": "https://example.com/boardml/core/phaseNode.schema.json"}}
  }
}