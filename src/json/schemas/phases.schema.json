{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "src/json/schemas/phases.schema.json",
  "title": "Phases",
  "type": "object",
  "required": [
    "initial",
    "nodes"
  ],
  "properties": {
    "initial": {
      "type": "string",
      "description": "TODO: Manual review - define initial."
    },
    "nodes": {
      "type": "object",
      "additionalProperties": {
        "$ref": "./phaseNode.schema.json"
      },
      "description": "TODO: Manual review - define nodes."
    }
  }
}
